

# IP address convention

- If we don't assign IP address to host/router, the emulator will automatically assign one
- Don't assign .1 address to any host or router, because this address is reserved by Docker
 

